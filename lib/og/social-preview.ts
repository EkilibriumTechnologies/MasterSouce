import { SITE_NAME } from "@/lib/site";

/** Dimensions of `/public/og-image.png` (square social card). */
export const SOCIAL_PREVIEW_SIZE = { width: 1024, height: 1024 } as const;

/** Alt text reflects the headline on the official social preview asset. */
export const SOCIAL_PREVIEW_ALT = `${SITE_NAME} — Fast mastering for independent artists, bedroom producers, and AI creators.`;
